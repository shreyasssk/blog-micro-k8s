name: ci

on:
  push:
    branches: main

jobs:
  multi-registries:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v2
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v1
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      -
        name: Login to Docker Hub
        uses: docker/login-action@v1 
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      -
        name: Build and push
        uses: docker/build-push-action@v2
        with:
          context: .
          file: |
            ./client/Dockerfile
            ./server/comments/Dockerfile
            ./server/event-bus/Dockerfile
            ./server/moderation/Dockerfile
            ./server/posts/Dockerfile
            ./server/query/Dockerfile
          platforms: linux/386,linux/amd64,linux/arm/v6,linux/arm/v7,linux/arm64,linux/ppc64le,linux/s390x
          push: true
          tags: |
            shreyasssk/blog/client:$GITHUB_SHA
            shreyasssk/blog/client:latest
            shreyasssk/blog/comments:$GITHUB_SHA
            shreyasssk/blog/comments:latest
            shreyasssk/blog/event-bus:$GITHUB_SHA
            shreyasssk/blog/event-bus:latest
            shreyasssk/blog/moderation:$GITHUB_SHA
            shreyasssk/blog/moderation:latest
            shreyasssk/blog/posts:$GITHUB_SHA
            shreyasssk/blog/posts:latest
            shreyasssk/blog/query:$GITHUB_SHA
            shreyasssk/blog/query:latest
